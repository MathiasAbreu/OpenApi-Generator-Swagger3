# OpenApi-Generator-Swagger3

O código gerado pelo openapi generator fica disponibilizado no caminho "target/generated-sources/openapi/src".

O .jar gerado pelo openapi generator fica disponibilizado no target.
